import { createStore } from 'vuex'
import movieModule from './modules/movie'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    movieModule
  }
})
